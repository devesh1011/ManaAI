# TiDB Cloud Environment Configuration
# Copy this to .env and fill in your actual values

# TiDB Cloud Database Connection
# Format: mysql+pymysql://username:password@host:port/database?ssl_ca=/path/to/ca-cert.pem
# Get this from your TiDB Cloud console -> Clusters -> Connect
DATABASE_URL=mysql+pymysql://your_username:your_password@your_tidb_host:4000/your_database?ssl_ca=/path/to/ca-cert.pem

# Google Gemini API Key  
# Get this from Google AI Studio: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_google_gemini_api_key_here

# Other existing settings (keep your current values)
SECRET_KEY=your_existing_secret_key
ALGORITHM=your_existing_algorithm
ACCESS_TOKEN_EXPIRE_MINUTES=your_existing_value

# TiDB Cloud Connection Tips:
# 1. Download the CA certificate from TiDB Cloud console
# 2. Place it in a secure location (e.g., ./certs/ca-cert.pem)
# 3. Use the full path in the ssl_ca parameter
# 4. Ensure your TiDB cluster allows connections from your IP
# 5. Use the correct username and password from your cluster settings

# Vector Search Configuration (optional, defaults are provided)
VECTOR_DIMENSION=768  # Google Gemini text-embedding-004 dimension
DEFAULT_TOP_K=10      # Default number of search results
SIMILARITY_THRESHOLD=0.1  # Minimum similarity score for results